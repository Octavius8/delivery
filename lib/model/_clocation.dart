class CLocation {
  String address = "";
  String name = "";
  double latitude;
  double longitude;

  CLocation(
      {required this.address,
      required this.name,
      required this.latitude,
      required this.longitude});
}
